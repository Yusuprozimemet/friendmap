# Test and lint tooling. Kept out of requirements.txt so the runtime image
# doesn't ship a linter — the Dockerfile installs requirements.txt only.
-r requirements.txt

ruff==0.8.6
pytest==8.3.4
# TestClient's transport. FastAPI needs it to drive the app in-process, which
# is what lets the API tests run without binding a port.
httpx==0.28.1